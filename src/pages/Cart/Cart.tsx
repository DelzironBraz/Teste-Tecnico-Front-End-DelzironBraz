import './Cart.scss';
import CartSection from '../../components/Cart/CartSection/CartSection';

const Cart = () => {
    return (
        <main className='cart'>
            <CartSection />
        </main>
    );
};

export default Cart;
